import { DeleteButton } from '../../utils/ui/CustomizedComponents';

const DeleteView = () => {
  return (
    <div>
      <DeleteButton
        style={{ width: '150px' }}
      >
        Delete
      </DeleteButton>
    </div>
  );
};

export default DeleteView;
